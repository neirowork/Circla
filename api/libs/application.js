/**
 * 申し込み情報を作成する
 * @param {string} circleHash
 * @param {string} paymoId
 */
export const create = (circleHash, paymoId) => {}

/**
 * 申し込み状態を更新する
 * @param {string} applicationHash
 * @param {string} status
 */
export const update = (applicationHash, status) => {}

/**
 * 申し込み状態を取得する
 * @param {string} applicationHash
 */
export const get = applicationHash => {}
